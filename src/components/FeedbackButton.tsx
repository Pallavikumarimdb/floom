'use client';
// TODO(v5-port): FeedbackButton stubbed. Original (floom@main) uses useSearchParams,
// useLocation from react-router-dom, and api.postFeedback(). In floom-minimal,
// migrate to useSearchParams from next/navigation and /api/feedback route.
// See docs/v5-port-stubs.md for full stub list.

export function FeedbackButton() {
  // Stub: renders nothing until v5-port migration is complete.
  return null;
}
