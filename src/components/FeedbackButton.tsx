'use client';
// useLocation from react-router-dom, and api.postFeedback(). In floom-minimal,
// migrate to useSearchParams from next/navigation and /api/feedback route.

export function FeedbackButton() {
  // Stub: renders nothing until internal port migration is complete.
  return null;
}
