{
  "type": "object",
  "required": ["topic"],
  "additionalProperties": false,
  "properties": {
    "topic": {
      "type": "string",
      "title": "Topic",
      "minLength": 1
    },
    "audience": {
      "type": "string",
      "title": "Audience",
      "default": "busy operator"
    },
    "tone": {
      "type": "string",
      "title": "Tone",
      "enum": ["direct", "warm", "technical"],
      "default": "direct"
    }
  }
}
