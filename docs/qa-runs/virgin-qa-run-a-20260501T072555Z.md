# Virgin QA Run A - Floom v0.1 Production Launch

- Timestamp: 2026-05-01T07:25:55Z to 2026-05-01T07:42Z
- Agent: Codex isolated QA
- Repo: `/tmp/floom-main-post-analytics`
- Commit: `31a45baa0826490bb7f04cc19ee6f2bd98cc5343`
- Production target: `https://floom.dev`
- Published CLI: `@floomhq/cli@0.2.16`
- Test slug: `qa-virgin-a-20260501072555-37c466`
- Public app URL: `https://floom.dev/p/qa-virgin-a-20260501072555-37c466`

## Scores

- Overall launch flow: 88/100
- Login/session/token flow: 85/100
- CLI auth/deploy/run: 90/100
- Public app browser run: 92/100
- Public API run: 96/100
- MCP `run_app`: 96/100
- 390px overflow: 94/100
- Cleanup: 55/100

## Pass/Fail Evidence

### `/login` and Google Sign-In Path

- PASS: Existing Chrome production session was available.
- PASS: Opening `https://floom.dev/login` while authenticated landed on `https://floom.dev/tokens`, showed `Signed in as depontefede@gmail.com`, and did not redirect to localhost.
- PASS: Fresh unauthenticated CDP browser context opened `https://floom.dev/login` and showed email/password plus `Continue with Google`.
- PASS: 390px unauthenticated `/login` check: `docScrollWidth=390`, `bodyScrollWidth=390`, `hasOverflow=false`.
- Screenshot hash: `b203dfec7d794c2a`, 35,659 bytes.

### `/tokens` and Agent Token Creation

- PASS: `/tokens` loaded in authenticated production session.
- PASS: New token was created through the UI, raw token appeared once, copy button reported copied, and refresh hid the raw value.
- PASS: Token list showed prefix metadata only. Redacted prefix: `flm_live_P__`.
- PASS: Created token was revoked after the run. Reloaded token list shows that prefix with `Status=Revoked`.
- PARTIAL: Revoked-token publish/run failure was not retested because the raw token was discarded after cleanup.
- Screenshot hash while raw token was visible: `5dc37fad3e87c251`, 114,889 bytes. Raw token was not logged.

### Published npm CLI Auth/Deploy/Run

- PASS: `npx -y @floomhq/cli@latest --version` returned `0.2.16`.
- PASS: `FLOOM_TOKEN=[REDACTED] FLOOM_API_URL=https://floom.dev npx -y @floomhq/cli@latest auth whoami --api-url https://floom.dev` returned identity `d3afb56b-a933-4ab5-b49a-4c4a38f785fa`.
- PASS: `deploy --dry-run` returned `ok` and targeted `POST https://floom.dev/api/apps`.
- PASS: `deploy` succeeded and published `Virgin QA Run A`.
- PASS: CLI printed canonical app page: `https://floom.dev/p/qa-virgin-a-20260501072555-37c466`.
- WARN: The deploy JSON payload also contained a non-canonical Vercel alias URL: `https://floom-60sec-mu.vercel.app/p/qa-virgin-a-20260501072555-37c466`.
- PASS: `floom run qa-virgin-a-20260501072555-37c466 ... --json --no-auth --api-url https://floom.dev` returned:
  - `execution_id=9e6804c3-8313-4b08-a455-affa632d6a4f`
  - `status=success`
  - `output.count=2`
  - due values: `friday`, `tomorrow`

### Public Browser App Run

- PASS: `https://floom.dev/p/qa-virgin-a-20260501072555-37c466` loaded and rendered the generated form.
- PASS: Browser run returned visible JSON output with `count=1`, a non-trivial action item, and runtime `2631 ms`.
- PASS: Empty required input disabled the `Run` button.
- PARTIAL: No explicit inline validation error text appeared for the empty required input; enforcement was by disabled submit state.

### Public API Run

- PASS: Anonymous metadata read:
  - `GET https://floom.dev/api/apps/qa-virgin-a-20260501072555-37c466`
  - status `200`
  - `public=true`
  - runtime `python`
- PASS: Anonymous run:
  - `POST https://floom.dev/api/apps/qa-virgin-a-20260501072555-37c466/run`
  - status `200`
  - `execution_id=dea61fc5-6a18-4fd6-a1f2-147280f31bac`
  - `status=success`
  - `output.count=2`
  - due values: `friday`, `tomorrow`

### MCP

- PASS: `GET https://floom.dev/mcp` returned:
  - `name=floom`
  - `endpoint=https://floom.dev/mcp`
  - `transport=json-rpc-over-http`
- PASS: `tools/list` returned tools:
  - `auth_status`
  - `get_app_contract`
  - `list_app_templates`
  - `get_app_template`
  - `validate_manifest`
  - `publish_app`
  - `find_candidate_apps`
  - `get_app`
  - `run_app`
- PASS: No `create_agent_token` tool was listed.
- PASS: Anonymous public `run_app` succeeded:
  - `execution_id=19e83469-c31e-434a-a11e-f9de053d0848`
  - `status=success`
  - `output.count=2`
  - due values: `friday`, `tomorrow`

### 390px Horizontal Overflow

- PASS: `/` at 390px: `docScrollWidth=390`, `bodyScrollWidth=390`, `hasOverflow=false`; screenshot hash `a633a29f78cf58bc`.
- PASS: unauthenticated `/login` at 390px: `docScrollWidth=390`, `bodyScrollWidth=390`, `hasOverflow=false`; screenshot hash `b203dfec7d794c2a`.
- PASS: authenticated `/tokens` at 390px: `docScrollWidth=390`, `bodyScrollWidth=390`, `hasOverflow=false`; screenshot hash `c6d23a696ddf7de4`.
- NOTE: `/tokens` contains a wide table inside an internal scroll container. It does not create document-level horizontal overflow.
- PASS: `/p/qa-virgin-a-20260501072555-37c466` at 390px: `docScrollWidth=390`, `bodyScrollWidth=390`, `hasOverflow=false`; screenshot hash `f50cfe3a91105584`.

### Cleanup

- PASS: Temporary local app directory under `/tmp/floom-qa-virgin-a-20260501072555-37c466` was removed.
- PASS: Created UI agent token was revoked.
- FAIL: Test app was not removed. No local `SUPABASE_SERVICE_ROLE_KEY`, `FLOOM_SUPABASE_SERVICE_ROLE_KEY`, or cleanup script was available.
- Coordinator cleanup slug: `qa-virgin-a-20260501072555-37c466`.

## Findings

1. CLI deploy response includes a Vercel alias in the JSON `app.url` while the human-readable CLI output uses canonical `https://floom.dev`. This is not a flow blocker, but launch output is inconsistent.
2. Required-field invalid browser input disables `Run`, but no explicit validation copy appeared in the observed state.
3. App cleanup requires coordinator/service-role cleanup because no local cleanup credential was available.

## Self-Audit

- Verified production browser paths with authenticated and fresh unauthenticated Chrome/CDP contexts.
- Verified CLI version, auth, dry-run deploy, deploy, and run against `https://floom.dev`.
- Verified public metadata, public API run, and MCP `run_app` with exact execution IDs.
- Verified 390px document-level overflow with CDP viewport and screenshot hashes.
- Verified no raw cookies, authorization headers, JWTs, or raw agent tokens are present in this log.
