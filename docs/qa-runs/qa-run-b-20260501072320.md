# Virgin QA Run B - Floom MCP/Agent Token Launch

Date: 2026-05-01 09:23:20 +0200
Target: https://floom.dev
Repo: `/tmp/floom-main-post-analytics`
Token handling: coordinator token file used as secret input only. No raw token is recorded here.

## Result

Overall: Pass
Score: 92/100

Functional MCP and published CLI launch paths worked from scratch against production. Deductions are for cleanup not being available from this isolated environment and for one MCP publish response field returning the legacy Vercel URL even though the canonical `https://floom.dev/p/:slug` page resolves.

## Live Apps Created

| Surface | Slug | Cleanup |
| --- | --- | --- |
| MCP publish_app | `qa-b-mcp-20260501072129-8hmbb5` | Requires coordinator cleanup |
| CLI deploy | `qa-b-cli-20260501072154-69c5` | Requires coordinator cleanup |

Cleanup was not performed because no local `SUPABASE_SERVICE_ROLE_KEY` / Supabase service-role env was present and the published CLI help exposed no app delete command. Local temp CLI HOME and app workspace were removed.

## MCP Evidence

Endpoint: `POST https://floom.dev/mcp`

| Check | Result | Evidence |
| --- | --- | --- |
| `tools/list` | Pass | HTTP 200, 9 tools returned. Required tools present: `get_app_contract`, `list_app_templates`, `get_app_template`, `validate_manifest`, `publish_app`, `run_app`. |
| `get_app_contract` | Pass | Returned `version: v0.1` and unsupported cases for FastAPI/OpenAPI, TypeScript/Node, multiple Python files, and multi-action manifests. |
| `list_app_templates` | Pass | Returned `invoice_calculator`, `utm_url_builder`, `csv_stats`, `meeting_action_items`. |
| `get_app_template(meeting_action_items)` | Pass | Returned files: `floom.yaml`, `app.py`, `input.schema.json`, `output.schema.json`, plus example transcript input. |
| `validate_manifest` | Pass | Unique v0.1 manifest for `qa-b-mcp-20260501072129-8hmbb5` returned `valid: true`; input and output schemas returned `valid`. |
| Negative unsupported candidate | Pass | FastAPI/OpenAPI candidate returned a clear rejection: `openapi.json is not supported in v0.1; OpenAPI/HTTP apps require the post-v0.1 HTTP app runner.` |
| `publish_app` | Pass | Published app slug `qa-b-mcp-20260501072129-8hmbb5`; response included `app` object. |
| `run_app` | Pass | Returned `status: success` with `summary: Extracted 3 action item(s).` and 3 action items. |

MCP publish response note: the response `app.url` was `https://floom-60sec-mu.vercel.app/p/qa-b-mcp-20260501072129-8hmbb5`. A follow-up anonymous check confirmed `https://floom.dev/p/qa-b-mcp-20260501072129-8hmbb5` returns HTTP 200 HTML and `https://floom.dev/api/apps/qa-b-mcp-20260501072129-8hmbb5` returns HTTP 200 metadata.

## CLI Evidence

Published CLI: `@floomhq/cli` v0.2.16

Commands exercised:

- `floom init`
- `floom auth login` with the token supplied from the coordinator secret file
- `floom auth whoami`
- `floom deploy`
- `floom run qa-b-cli-20260501072154-69c5 '{"text":"CLI QA B verifies published run path with redacted evidence"}' --json`

| Check | Result | Evidence |
| --- | --- | --- |
| CLI auth | Pass | Noninteractive auth succeeded against `https://floom.dev`; identity resolved. Token preview output redacted from this log. |
| CLI deploy | Pass | Published `qa-b-cli-20260501072154-69c5`; CLI printed canonical app page `https://floom.dev/p/qa-b-cli-20260501072154-69c5`. |
| CLI run | Pass | Returned `status: success`, output `summary: CLI QA B verifies published run path with redacted evidence`, `word_count: 10`, `error: null`. |
| Anonymous production resolution | Pass | `https://floom.dev/api/apps/qa-b-cli-20260501072154-69c5` returned HTTP 200 metadata; `https://floom.dev/p/qa-b-cli-20260501072154-69c5` returned HTTP 200 HTML. |

One CLI negative during the run: the first run used the wrong scaffold input key (`name` instead of required `text`) and returned HTTP 400 `Invalid input`. The corrected schema-valid input passed.

## Verification Summary

- Product source was not edited.
- Only this redacted QA run log was added under the repo.
- Token-like values are omitted or replaced with `[REDACTED]`.
- Production MCP and CLI paths were verified with live publish and live run calls.
- Public app metadata and page routes were verified anonymously for both unique slugs.
