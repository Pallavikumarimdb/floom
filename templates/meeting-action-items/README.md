# Meeting Action Items

v0.1-safe Floom template.

```bash
cd templates/meeting-action-items
npx @floomhq/cli@latest deploy --dry-run
npx @floomhq/cli@latest deploy
```

Before publishing your own copy, change `slug` in `floom.yaml` to a unique value.
